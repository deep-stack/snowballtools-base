{
  "name": "snowball-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "lerna run lint --stream -- --max-warnings=0"
  }
}
