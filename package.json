{
  "name": "snowball-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "depcheck": "^1.4.2",
    "husky": "^8.0.3",
    "lerna": "^8.0.0",
    "patch-package": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream"
  }
}